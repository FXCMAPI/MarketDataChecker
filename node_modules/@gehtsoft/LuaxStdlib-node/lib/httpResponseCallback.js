'use strict';
//@@DocBrief("Callback class for processing HTTP response")
class CtdtrlHttpResponseCallback {
    onComplete(status, responseText) {
        return void(0);
    }
    onError(error) {
        return void(0);
    }
}
let httpResponseCallback = CtdtrlHttpResponseCallback;
module.exports = httpResponseCallback
