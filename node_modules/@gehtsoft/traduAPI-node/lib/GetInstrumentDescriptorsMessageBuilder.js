'use strict';
let GetInstrumentDescriptorsMessage = require('./GetInstrumentDescriptorsMessage.js');
let stdlib = require('@gehtsoft/LuaxStdlib-node').stdlib;
class GetInstrumentDescriptorsMessageBuilder extends GetInstrumentDescriptorsMessage {
    addInstrumentDescriptor(instrument) {
        this.instrumentsList.add(instrument);
    }
    addInstrumentDescriptors(instruments) {
        let i;
        if (this.instrumentsList == null) {
            stdlib.print("instrumentsList == nil");
        }
        if (instruments != null) {
            for (i = 0; i <= instruments.length() - 1; i += 1) {
                this.instrumentsList.add(instruments.get(i));
            }
        }
    }
    build() {
        return this;
    }
}
module.exports = GetInstrumentDescriptorsMessageBuilder
