'use strict';
let MessageType = require('./MessageType.js');
let InstrumentDescriptorsList = require('./InstrumentDescriptorsList.js');
class GetInstrumentDescriptorsMessage {
    instrumentsList;
    constructor() {
        this.instrumentsList = new InstrumentDescriptorsList();
    }
    getInstrumentDescriptors() {
        return this.instrumentsList.toArray();
    }
    getType() {
        return MessageType.GetInstrumentDescriptors;
    }
}
module.exports = GetInstrumentDescriptorsMessage
