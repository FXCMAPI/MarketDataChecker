'use strict';
class KeepAliveTradingSessionTransportMessageFactory {
    create(messageFactory, session, requestNumber, priceTerminal) {
        return messageFactory.createKeepAliveRequestMessage(session, requestNumber);
    }
}
module.exports = KeepAliveTradingSessionTransportMessageFactory
