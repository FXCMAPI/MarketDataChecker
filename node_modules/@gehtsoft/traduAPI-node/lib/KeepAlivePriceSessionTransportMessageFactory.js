'use strict';
class KeepAlivePriceSessionTransportMessageFactory {
    create(messageFactory, session, requestNumber, priceTerminal) {
        return messageFactory.createKeepAlivePricesRequestMessage(session, priceTerminal, requestNumber);
    }
}
module.exports = KeepAlivePriceSessionTransportMessageFactory
