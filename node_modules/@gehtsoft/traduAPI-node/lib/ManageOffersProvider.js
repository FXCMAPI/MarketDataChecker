'use strict';
class ManageOffersProvider {
    manager;
    constructor() {
        this.manager = null;
    }
    setOffersManager(manager) {
        this.manager = manager;
    }
    removeOfferById(offerId) {
        this.manager.removeOfferById(offerId);
    }
}
module.exports = ManageOffersProvider
