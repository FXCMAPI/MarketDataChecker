'use strict';
let PriceFeedBatchCommand = require('./PriceFeedBatchCommand.js');
class PriceFeedBatchCommandBuilder extends PriceFeedBatchCommand {
    setSessionStorage(sessionStorage) {
        this.sessionStorage = sessionStorage;
    }
    setMessageExecutor(messageExecutor) {
        this.messageExecutor = messageExecutor;
    }
    setMessageFactory(messageFactory) {
        this.messageFactory = messageFactory;
    }
    setRequestNumberGenerator(requestNumberGenerator) {
        this.requestNumberGenerator = requestNumberGenerator;
    }
    setCommandDescriptors(descriptors) {
        this.descriptors = descriptors;
    }
    setPriceFeedCommandCallback(priceFeedCommandCallback) {
        this.priceFeedCommandCallback = priceFeedCommandCallback;
    }
    setForceReset(forceReset) {
        this.forceReset = forceReset;
    }
    build() {
        return this;
    }
}
module.exports = PriceFeedBatchCommandBuilder
