'use strict';
let PdasGetTemporarySessionRequestMessage = require('./PdasGetTemporarySessionRequestMessage.js');
let TransportMessageType = require('./TransportMessageType.js');
class PdasGetTemporaryPriceSessionBySsoTokenRequestMessage {
    build(ssoToken, login, applicationName, agent, terminal, priceTerminal, tradingSession, requestNumberGenerator, uniqueString) {
        let message;
        message = new PdasGetTemporarySessionRequestMessage();
        return message.build("", ssoToken, login, applicationName, agent, terminal, priceTerminal, tradingSession, TransportMessageType.GetTemporaryPriceSession, 1022, requestNumberGenerator, uniqueString, "", [""]);
    }
}
module.exports = PdasGetTemporaryPriceSessionBySsoTokenRequestMessage
