'use strict';
let GetAllOffersCommand = require('./GetAllOffersCommand.js');
//@@DocBrief("The builder of the KeepAliveCommand class")
class GetAllOffersCommandBuilder extends GetAllOffersCommand {
    setOfferIds(offerIds) {
        this.offerIds = offerIds;
    }
    setMessageExecutor(messageExecutor) {
        this.messageExecutor = messageExecutor;
    }
    setTradingSession(tradingSession) {
        this.tradingSession = tradingSession;
    }
    setMessageFactory(messageFactory) {
        this.messageFactory = messageFactory;
    }
    setRequestNumber(requestNumber) {
        this.requestNumber = requestNumber;
    }
    build() {
        return this;
    }
}
module.exports = GetAllOffersCommandBuilder
