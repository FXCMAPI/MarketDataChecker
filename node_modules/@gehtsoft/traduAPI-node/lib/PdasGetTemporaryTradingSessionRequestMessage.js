'use strict';
let PdasGetTemporarySessionRequestMessage = require('./PdasGetTemporarySessionRequestMessage.js');
let TransportMessageType = require('./TransportMessageType.js');
class PdasGetTemporaryTradingSessionRequestMessage {
    build(saltedPassword, login, applicationName, agent, terminal, requestNumberGenerator, uniqueString, configFlags) {
        let message;
        message = new PdasGetTemporarySessionRequestMessage();
        return message.build(saltedPassword, "", login, applicationName, agent, terminal, null, "", TransportMessageType.GetTemporaryTradingSession, 0, requestNumberGenerator, uniqueString, "", configFlags);
    }
}
module.exports = PdasGetTemporaryTradingSessionRequestMessage
