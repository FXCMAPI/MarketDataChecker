'use strict';
let GetTemporarySessionMessage = require('./GetTemporarySessionMessage.js');
//@@DocBrief("The builder of the GetTemporarySessionMessage class")
class GetTemporarySessionMessageBuilder extends GetTemporarySessionMessage {
    setTemporarySession(temporarySession) {
        this.temporarySession = temporarySession;
    }
    setError(error) {
        this.error = error;
    }
    build() {
        return this;
    }
}
module.exports = GetTemporarySessionMessageBuilder
