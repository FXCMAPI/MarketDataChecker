'use strict';
let list = require('@gehtsoft/LuaxStdlib-node').list;
class RequiredCommandsList extends list {
    get(index) {
        return super.get(index);
    }
    add(value) {
        super.add(value);
    }
}
module.exports = RequiredCommandsList
