'use strict';
let CreateOrderMessage = require('./CreateOrderMessage.js');
class CreateOrderMessageBuilder extends CreateOrderMessage {
    setOrderId(orderId) {
        this.orderId = orderId;
    }
    setOfferId(offerId) {
        this.offerId = offerId;
    }
    setError(error) {
        this.error = error;
    }
    setRequestTxt(requestTxt) {
        this.requestTxt = requestTxt;
    }
    build() {
        return this;
    }
}
module.exports = CreateOrderMessageBuilder
