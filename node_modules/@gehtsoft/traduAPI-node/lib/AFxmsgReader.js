'use strict';
let MessageBuilder = require('./MessageBuilder.js');
class AFxmsgReader {
    read(responseList, xmlRootNode, messages) {
        return void(0);
    }
    createLockedMessage(message) {
        if (message != null) {
            return MessageBuilder.createLockedMessage(message);
        }
        return null;
    }
}
module.exports = AFxmsgReader
