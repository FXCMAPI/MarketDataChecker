'use strict';
let GetSsoTokenCommand = require('./GetSsoTokenCommand.js');
class GetSsoTokenCommandBuilder extends GetSsoTokenCommand {
    setMessageExecutor(messageExecutor) {
        this.messageExecutor = messageExecutor;
    }
    setTradingSession(tradingSession) {
        this.tradingSession = tradingSession;
    }
    setMessageFactory(messageFactory) {
        this.messageFactory = messageFactory;
    }
    setRequestNumber(requestNumber) {
        this.requestNumber = requestNumber;
    }
    setApplicationName(applicationName) {
        this.applicationName = applicationName;
    }
    setProviderName(providerName) {
        this.providerName = providerName;
    }
    build() {
        return this;
    }
}
module.exports = GetSsoTokenCommandBuilder
