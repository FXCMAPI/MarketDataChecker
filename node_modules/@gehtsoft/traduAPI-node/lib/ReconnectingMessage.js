'use strict';
let MessageType = require('./MessageType.js');
//@@DocBrief("The class for for getting response on reconnecting message")
class ReconnectingMessage {
    getType() {
        return MessageType.Reconnecting;
    }
}
module.exports = ReconnectingMessage
