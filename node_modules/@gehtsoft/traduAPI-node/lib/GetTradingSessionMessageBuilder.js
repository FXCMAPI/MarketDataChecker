'use strict';
let GetTradingSessionMessage = require('./GetTradingSessionMessage.js');
//@@DocBrief("The builder of the GetTradingSessionMessage class")
class GetTradingSessionMessageBuilder extends GetTradingSessionMessage {
    setPersistentSession(persistentSession) {
        this.persistentSession = persistentSession;
    }
    setUserKind(userKind) {
        this.userKind = userKind;
    }
    setUserId(userId) {
        this.userId = userId;
    }
    setError(error) {
        this.error = error;
    }
    setOptions(options) {
        this.options = options;
    }
    setRequestId(requestId) {
        this.requestId = requestId;
    }
    build() {
        return this;
    }
}
module.exports = GetTradingSessionMessageBuilder
