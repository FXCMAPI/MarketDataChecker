'use strict';
let LoginController = require('./LoginController.js');
class LoginControllerFactory {
    create(commandEnvironment, commandFactory, communicator, connectionStatusManager, sessionStorage, ssoTokenController, aliveControllerFactory, rakeOutControllerFactory, instrumentsProvider, dxfeedConnectionFactory, systemSettingsProvider, testRequestControllerFactory) {
        return LoginController.create(commandEnvironment, commandFactory, communicator, connectionStatusManager, sessionStorage, ssoTokenController, aliveControllerFactory, rakeOutControllerFactory, instrumentsProvider, dxfeedConnectionFactory, systemSettingsProvider, testRequestControllerFactory);
    }
}
module.exports = LoginControllerFactory
