'use strict';
let PdasGetTemporarySessionRequestMessage = require('./PdasGetTemporarySessionRequestMessage.js');
let TransportMessageType = require('./TransportMessageType.js');
class PdasGetTemporaryTradingSessionBySsoTokenRequestMessage {
    build(ssoToken, login, applicationName, agent, terminal, requestNumberGenerator, uniqueString, ssoTokenProvider, configFlags) {
        let message;
        message = new PdasGetTemporarySessionRequestMessage();
        return message.build("", ssoToken, login, applicationName, agent, terminal, null, "", TransportMessageType.GetTemporaryTradingSession, 0, requestNumberGenerator, uniqueString, ssoTokenProvider, configFlags);
    }
}
module.exports = PdasGetTemporaryTradingSessionBySsoTokenRequestMessage
