'use strict';
let TestRequestController = require('./TestRequestController.js');
class TestRequestControllerFactory {
    subscriptionsMdtRecover;
    createTestRequestPrices(commandFactory, connectionParameters, sessionProvider, messageFactory, messageExecutor, messageRouter, requestNumberGenerator, periodInMilliseconds, priceTerminal, testRequestControllerCallback) {
        let testRequestCommand;
        testRequestCommand = commandFactory.createTestRequestPricesCommand(periodInMilliseconds, connectionParameters, messageRouter, messageExecutor, messageFactory, requestNumberGenerator, priceTerminal);
        return TestRequestController.create(sessionProvider, messageRouter, testRequestCommand, testRequestControllerCallback, this.subscriptionsMdtRecover);
    }
    createTestRequestTrading(commandFactory, connectionParameters, sessionProvider, messageFactory, messageExecutor, messageRouter, requestNumberGenerator, periodInMilliseconds, testRequestControllerCallback) {
        let testRequestCommand;
        testRequestCommand = commandFactory.createTestRequestTradingCommand(periodInMilliseconds, connectionParameters, messageRouter, messageExecutor, messageFactory, requestNumberGenerator);
        return TestRequestController.create(sessionProvider, messageRouter, testRequestCommand, testRequestControllerCallback, null);
    }
    setSubscriptionsMdtRecover(subscriptionsMdtRecover) {
        this.subscriptionsMdtRecover = subscriptionsMdtRecover;
    }
}
module.exports = TestRequestControllerFactory
