'use strict';
let GetPriceSessionMessage = require('./GetPriceSessionMessage.js');
//@@DocBrief("The builder of the GetPriceSessionMessage class")
class GetPriceSessionMessageBuilder extends GetPriceSessionMessage {
    setPersistentSession(persistentSession) {
        this.persistentSession = persistentSession;
    }
    setUserKind(userKind) {
        this.userKind = userKind;
    }
    setUserId(userId) {
        this.userId = userId;
    }
    setError(error) {
        this.error = error;
    }
    setOptions(options) {
        this.options = options;
    }
    setRequestId(requestId) {
        this.requestId = requestId;
    }
    build() {
        return this;
    }
}
module.exports = GetPriceSessionMessageBuilder
