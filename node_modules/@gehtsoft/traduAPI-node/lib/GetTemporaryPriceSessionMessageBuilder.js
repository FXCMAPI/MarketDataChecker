'use strict';
let GetTemporaryPriceSessionMessage = require('./GetTemporaryPriceSessionMessage.js');
//@@DocBrief("The builder of the GetTemporaryPriceSessionMessage class")
class GetTemporaryPriceSessionMessageBuilder extends GetTemporaryPriceSessionMessage {
    setTemporaryPriceSession(temporarySession) {
        this.temporarySession = temporarySession;
    }
    setError(error) {
        this.error = error;
    }
    setRequestId(requestId) {
        this.requestId = requestId;
    }
    build() {
        return this;
    }
}
module.exports = GetTemporaryPriceSessionMessageBuilder
